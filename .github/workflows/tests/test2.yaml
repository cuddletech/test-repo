name: Test 2

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Do something
        run: echo "Now I will do the deploy to prod!!!!"
